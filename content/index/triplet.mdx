import { jsx } from 'theme-ui'
import { Box, Grid } from '@theme-ui/components'
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'
import {
  faUsersCrown,
  faPhoneLaptop,
  faStore,
} from '@fortawesome/pro-duotone-svg-icons'
import { FaUsersCrown, FaPhoneLaptop, FaStore } from '../../components/icons'
import IconWrapper from './icon-wrapper'

<div>

### <FaUsersCrown wrapper={IconWrapper} /> Focus on Users

First and foremost, let's think about how to help our users do what they need to
do.

</div>
<div>

### <FaPhoneLaptop wrapper={IconWrapper} /> Write Healthy Code

Then, let's write some clean code to make it happen. Perfect is good;
done is better.

</div>
<div>

### <FaStore wrapper={IconWrapper} /> Create Lasting Value

Finally, let's cultivate healthy relationships with each other and our customers.

</div>
